{
  "name": "datasilk.sql",
  "version": "1.0.0",
  "description": "PostgreSQL schema deployment generator for the Datasilk framework.",
  "private": true,
  "scripts": {
    "update-deploy": "gulp update-deploy",
    "watch": "gulp watch"
  },
  "devDependencies": {
    "gulp": "^5.0.0"
  }
}
